# dual-secret-acceptance
Dual Secret Acceptance (DSA) is a method for authorizing actions based on the satisfaction of two independent acceptance conditions (“secrets”), rather than on a single credential or factor. 
